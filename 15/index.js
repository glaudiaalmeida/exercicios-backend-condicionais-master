const primeiroNome = "";
const sobrenome = "";
const apelido = "";

if ((primeiroNome) || (sobrenome) || (apelido)) {
    console.log(primeiroNome + " " + sobrenome + " " + apelido);
} else {
    console.log("Preencha um dos campos!");
}